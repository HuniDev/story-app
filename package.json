{
	"name": "backend",
	"version": "1.0.0",
	"description": "social story app",
	"main": "server.js",
	"scripts": {
		"start": "cross-env NODE_ENV=production node server",
		"dev": "cross-env NODE_ENV=development nodemon server",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "hunidev",
	"license": "ISC",
	"dependencies": {
		"connect-mongo": "^4.6.0",
		"dotenv": "^16.0.0",
		"express": "^4.18.1",
		"express-handlebars": "^6.0.5",
		"express-session": "^1.17.2",
		"method-override": "^3.0.0",
		"moment": "^2.29.3",
		"mongoose": "^6.3.2",
		"morgan": "^1.10.0",
		"passport": "^0.5.2",
		"passport-google-oauth20": "^2.0.0"
	},
	"devDependencies": {
		"cross-env": "^7.0.3",
		"nodemon": "^2.0.16"
	}
}
